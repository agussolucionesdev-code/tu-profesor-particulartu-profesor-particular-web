# Issues iniciales sugeridos para GitHub

## Issue 1 — Migrar landing estática a React manteniendo fidelidad 1:1
Usar `reference-static-site/` como fuente de verdad. Crear `frontend/` con React + Vite y replicar secciones, assets, estilos e interacciones.

## Issue 2 — Crear backend Node/Express mínimo
Crear `backend/` con endpoints `/health`, `/api/leads`, `/api/contact`, validación, CORS, Helmet y rate limit.

## Issue 3 — Preparar deploy frontend en Vercel
Configurar build, variables de entorno y README para desplegar `frontend/` en Vercel.

## Issue 4 — Preparar deploy backend en Render
Configurar start command, env vars y README para desplegar `backend/` en Render.

## Issue 5 — Optimizar SEO, accesibilidad y performance
Agregar metadatos, Open Graph, sitemap/robots si corresponde, alt text, contraste, lazy loading y revisión Lighthouse.
